server:
  port: 8080

spring:
  application:
    name: HOTEL-K8S-DEMO

eureka:
  instance:
    preferIpAddress: true
    hostname: eureka
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://eureka:8761/eureka